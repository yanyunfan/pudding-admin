module.exports = {
  // 指定最大换行长度，默认80
  printWidth: 130,
  // 指定缩进空格数，默认2
  tabWidth: 2,
  // 使用制表符而不是空格缩进行，默认false
  useTabs: false,
  // 在语句末尾添加分号，默认true
  semi: true,
  // 使用单引号而不是双引号，默认false
  singleQuote: false,
  // 多行时尽可能打印尾随逗号，可选值<es5|none|all>，默认es5
  trailingComma: "none",
  // 对象字面量中括号之间的空格，默认true
  bracketSpacing: true,
  // 将 > 多行元素放在最后一行的末尾，而不是单独放在下一行，默认false
  bracketSameLine: true,
  // (x) => {} 箭头函数参数只有一个时是否要有小括号，可选值<always|avoid>
  arrowParens: "avoid",
  // 行尾换行符，可选值<lf|crlf|cr|auto>，默认lf
  endOfLine: "lf"
};
